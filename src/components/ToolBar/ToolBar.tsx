// import { Loading } from "../Loading"


export function ToolBar() {
    <div className="ToolBar">
        {/* {loading ? (
                <Loading/>
            ) : (
                nameDB && nameDB.length > 0 ? (
                nameDB.map((item, index) => <p key={index}>{item.name}</p>) // Отображаем данные
                ) : (
                <p>No data found</p> // Показываем сообщение, если данных нет
                )
            )} */}
    </div>
}